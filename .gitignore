node_modules/
dist/
.wrangler/
.dev.vars
*.log

# Credentials / secrets — NEVER commit these
Generated API key.txt
*.env
.env
.env.*
*.secret
*credentials*
# ...except the values-free registry doc, which is meant to be committed.
!CREDENTIALS.md

# Drizzle generated migrations are committed; local D1 state is not.
.mf/

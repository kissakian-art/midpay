# LiveKit's RN packages declare loose/legacy peer ranges, so a strict npm
# install (e.g. on the EAS build server) fails to resolve them. Match what we
# use locally so CI installs the same tree. No auth tokens here — safe to commit.
legacy-peer-deps=true
